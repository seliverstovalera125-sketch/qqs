<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - <%= botUsername %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gray-900 text-gray-100">
    <div class="container mx-auto px-4 py-8">
        <header class="mb-8">
            <h1 class="text-3xl font-bold mb-2">📊 Dashboard - <%= botUsername %></h1>
            <p class="text-gray-400">Статистика серверов в реальном времени</p>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% servers.forEach(server => { %>
            <div class="bg-gray-800 rounded-xl p-6 border border-gray-700">
                <div class="flex items-center mb-4">
                    <% if(server.icon) { %>
                        <img src="<%= server.icon %>" alt="<%= server.name %>" class="w-12 h-12 rounded-full mr-3">
                    <% } %>
                    <div>
                        <h3 class="font-bold text-lg"><%= server.name %></h3>
                        <p class="text-gray-400 text-sm">ID: <%= server.id %></p>
                    </div>
                </div>

                <div class="space-y-4">
                    <div>
                        <h4 class="font-semibold mb-2 text-blue-400">👥 Участники</h4>
                        <div class="grid grid-cols-2 gap-2">
                            <div class="bg-gray-700 p-2 rounded">
                                <p class="text-sm text-gray-400">Всего</p>
                                <p class="font-bold"><%= server.stats.members.total %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded">
                                <p class="text-sm text-gray-400">Онлайн</p>
                                <p class="font-bold text-green-400"><%= server.stats.members.online %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded">
                                <p class="text-sm text-gray-400">Люди</p>
                                <p class="font-bold"><%= server.stats.members.humans %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded">
                                <p class="text-sm text-gray-400">Боты</p>
                                <p class="font-bold text-purple-400"><%= server.stats.members.bots %></p>
                            </div>
                        </div>
                    </div>

                    <div>
                        <h4 class="font-semibold mb-2 text-green-400">📁 Каналы</h4>
                        <div class="grid grid-cols-3 gap-2">
                            <div class="bg-gray-700 p-2 rounded text-center">
                                <p class="text-sm text-gray-400">Текст</p>
                                <p class="font-bold"><%= server.stats.channels.text %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded text-center">
                                <p class="text-sm text-gray-400">Голос</p>
                                <p class="font-bold"><%= server.stats.channels.voice %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded text-center">
                                <p class="text-sm text-gray-400">Всего</p>
                                <p class="font-bold"><%= server.stats.channels.total %></p>
                            </div>
                        </div>
                    </div>

                    <div>
                        <h4 class="font-semibold mb-2 text-yellow-400">✨ Другое</h4>
                        <div class="grid grid-cols-3 gap-2">
                            <div class="bg-gray-700 p-2 rounded text-center">
                                <p class="text-sm text-gray-400">Роли</p>
                                <p class="font-bold"><%= server.stats.other.roles %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded text-center">
                                <p class="text-sm text-gray-400">Эмодзи</p>
                                <p class="font-bold"><%= server.stats.other.emojis %></p>
                            </div>
                            <div class="bg-gray-700 p-2 rounded text-center">
                                <p class="text-sm text-gray-400">Бусты</p>
                                <p class="font-bold text-pink-400"><%= server.stats.other.boosts %></p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-4 pt-4 border-t border-gray-700">
                    <p class="text-sm text-gray-400">
                        <i class="fas fa-clock mr-1"></i>
                        Обновлено: <%= new Date(server.updated).toLocaleString() %>
                    </p>
                </div>
            </div>
            <% }); %>
        </div>

        <% if(servers.length === 0) { %>
        <div class="text-center py-12">
            <i class="fas fa-server text-6xl text-gray-700 mb-4"></i>
            <p class="text-xl text-gray-400">Нет данных о серверах</p>
            <p class="text-gray-500">Бот должен быть добавлен хотя бы на один сервер</p>
        </div>
        <% } %>
    </div>

    <script>
        // Авто-обновление каждые 30 секунд
        setInterval(() => {
            location.reload();
        }, 30000);
    </script>
</body>
</html>